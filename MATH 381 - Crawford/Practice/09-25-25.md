### Practice problems in office hours

##### 1.8 exercise #5 (1.8.5)
Prove that if $x, y \in \mathbb{R}$, then $x + y = min(x, y) + max(x, y)$

**-PF-**
Assume $x, y \in \mathbb{R}$.
The minimum of two numbers is the smaller of the two numbers.
The maximum of two numbers is the larger of two numbers.
Let $a, b$ be arbitrary numbers where $a > b$. 
$min(a, b) + max(a, b) = b + a