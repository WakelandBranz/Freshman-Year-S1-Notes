## Definitions

### **Def. Set:** A *set* $S$ is an unordered collection of things called *elements*. We say a set *contains* its elements, and write $a \in S$. We write $a \not \in S$, if $a$ is not an element of $S$.

### **Describing Sets**

#### **Rosters:** $\{Bob, Matt, Joe\}$, $\{..., -3, -2, -1, 0 , 1, 2, 3, ...\}$ - Just an array-like list in curly braces.

===How do I use set builder notation in LaTeX? Ask Matt.===
===ASK MATT: IS $\{\emptyset, \emptyset\} = \{\emptyset\}$???===
#### **Set Builder Notation:** $S = \{\frac{a}{b} \mid a, b \in \mathbb{Z}, b \neq 0\} = \mathbb{Q}$ (rational numbers)
"the set of all x such that x has property P"

##### Further examples:
- ***ODDS:***
	- $\{x \in \mathbb{Z} \mid \text{x is odd}\}$
	- $\{x \in \mathbb{R} \mid \text{x is an odd integer} \}$
	- $\{x \mid \exists k \in \mathbb{Z}|x = 2k + 1\}$
#### **Intervals:** 
- $[0, 1) = \{x \in \mathbb{R} \mid 0 \leq x < 1\}$ 
- $[a, b] = \{x \mid a \leq x \leq b\}$
- $[a, b) = \{x \mid a \leq x < b\}$
- $(a, b] = \{x \mid a < x \leq b\}$
- $(a, b) = \{x \mid a < x < b\}$
	- $(a, b)$ - Open interval
	- $[a, b]$ - Closed interval
### **Often Used Sets:**

- $\mathbb{N} = \{1, 2, 3, \ldots\}$ the set of natural numbers (what you count with)
- $\mathbb{Z} = \{\ldots, -2, -1, 0, 1, 2, \ldots\}$ the set of integers
- $\mathbb{Z}^+ = \{1, 2, 3, \ldots\}$ the set of positive integers
- $\mathbb{Q} = \left\{\frac{a}{b} \mid a, b \in \mathbb{Z} \text{ and } b \neq 0\right\}$ the set of rational numbers
- $\mathbb{Q}^+ = \left\{\frac{a}{b} \mid a, b \in \mathbb{Z} \text{ and } b \neq 0 \text{ and } \frac{a}{b} > 0\right\}$ the set of positive rational numbers
- $\mathbb{R}$ the set of real numbers
- $\mathbb{R}^+$ the set of positive real numbers
- $\mathbb{C}$ the set of complex numbers

### **Def. The Empty Set:** A set that has no elements. can be represented as $\emptyset$ or $\{\hspace{2mm}\}$.

Think of the empty set as a folder that is empty.
##### **Example**
- $\{x \in \mathbb{Z} \mid 0 < x < 1\} = \emptyset$

### **Def. Singleton Sets:** Set with exactly one element. Can be $\{\emptyset\}$ but $\{\emptyset\} \neq \emptyset$

Think of the $\{\emptyset\}$ as a folder containing the folder that is empty.
##### **Example**
 - $\{\{x \in \mathbb{Z} \mid 0 < x < \frac{1}{n}\} \mid n \in \mathbb{Z^+}\} = \{\emptyset\}$

---

## Examples

### **Ex 1.** Determine how many elements are in the set $\{N, Z, Q, R\}$.

**Answer:** 4 elements are in the set $\{N, Z, Q, R\} \text{ - This contains 4 sets.}$

### **Ex 2.** Are ∅ and {∅} the same set? Why or why not?

**Answer:** No, they are not. $\emptyset$ can be represented as $\{\hspace{2mm} \}$ but $\{\emptyset\}$ is the singleton set.

### Ex 3. Is $\{1, 1, 2, 3\} = \{1, 2, 3\}$ true?

**Answer:** Yes, this is true. Sets are assumed to be deduplicated.
 - Fancy math way of saying: $A = B \leftrightarrow \forall x (x \in A \leftrightarrow x \in B)$

### Ex 4. Is $\{\emptyset, \emptyset\} = \{\emptyset\}$ true?
 - Yes, this is true!

---

### **Def. Equality of Sets:**

### **Ex 3.** Determine which of the following sets are equal to {1, 3, 5}:

(a) {1, 1, 3, 3, 5, 5}  
**Answer:** True

(b) {∅, 1, 3, 5}  
**Answer:** False

(c) {3, 1, 5, 1}  
**Answer:** True

(d) {{1}, {3}, {5}}  
**Answer:** False

---

### **Def. The Universal Set:** the set that contains **all objects under consideration** in a particular discussion or problem.
 - A set $A$ is a subset of $B$, written $A \subseteq B$, means every element of $A$ is an element of $B$.

### **Def. Subset and Superset:**

#### Subset

Set A is a **subset** of set B (written A $\subseteq$ B) if every element of A is also an element of B.

#### Superset

Set B is a **superset** of set A (written B $\supseteq$ A) if B contains all elements of A.

### **Ex 1.** Determine which of the following sets are subsets/supersets of the others: N, Z, Z⁺, Q, Q⁺, R, R⁺, C

**Answer:**

### Ex 2. How to show a set $A \not \subseteq B$?
 - Find an element in $A$ that is not $B$.

### **Theorem:** For any set A:
1. $\emptyset\ \subseteq A$
2. $A \subseteq A$

**-PF-**
Want to show $\forall x(x \in \emptyset \rightarrow x \in A)$.
This is a tautology.
Want to show $\forall x (x \in A \rightarrow x \in A)$.
This is trivial.

---
## SIMPLE EXPLANATION

### Subset

A **subset** is like a smaller group that fits completely inside a larger group. If every person in group A is also in group B, then A is a subset of B.

**Example:** If A = {cats, dogs} and B = {cats, dogs, birds}, then A is a subset of B because all animals in A are also in B.

### Superset

A **superset** is just the opposite - it's the larger group that contains the smaller one. If B contains all the elements of A (and maybe more), then B is a superset of A.

**Example:** Using the same sets above, B = {cats, dogs, birds} is a superset of A = {cats, dogs} because B contains everything A has, plus more.

Think of it like Russian nesting dolls - the smaller doll (subset) fits completely inside the larger doll (superset).

---

### **Def. Proper Subset and Proper Superset:**

### **Theorem:** For any two sets A and B,

---

### **Def. Finite Set:**

### **Def. Power Set:**

### **Ex 5.** Determine the power sets P({0, 1, 2}), P(∅), and P({∅}).

**P({0, 1, 2}) =**

**P(∅) =**

**P({∅}) =**

### **Prop.** If |S| = n, then |P(S)| =

---

### **Def. Ordered n-tuple:**

### **Def. Cartesian Product:**

### **Ex 6.** Find the Cartesian product {1, 2} × {a, b, c}.

**Answer:**

---

### **Def. Relation:**

### **Ex 7.** Write the ordered pairs in the "less than or equal to" relation on the set {0, 1, 2, 3}.

**Answer:**

---

## Notes Section

_Use this space for additional notes, clarifications, or connections:_