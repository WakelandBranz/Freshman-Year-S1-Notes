## Overview

In most of our daily life, and in math, we do not argue as formally as we have done in the previous sections. Typically we do not so explicitly state when we use certain rules of inference or logical equivalences. Using these technical details would only muddle the ideas of an argument.

## Mathematical Terminology

### Key Definitions

**Theorem:** A statement that can be shown to be true. (usually important)
 - Theorems can also be referred to as facts or results.

**Proposition:** A less important theorem.

**Axioms:** Statements we assume to be true.
 - Used in proofs
 - AKA **postulates**

**Proof:** A valid argument showing a theorem is true

**Lemma:** A proposition usually used to prove a theorem.
 - Complicated proofs are usually easier to understand when they are proved using a series of lemmas, where each lemma is proved individually.

**Corollary:** A theorem that directly/immediately follows from another theorem.

**Conjecture:** A statement with supporting evidence that is thought to be true but has not been proven.
 - When proof of a **conjecture** is found, the **conjecture** becomes a theorem.

> [!note] Course Expectation In this class you should... _(incomplete in source)_

---

## Methods of Proofs

This section covers three basic approaches to a proof:

### 1. Direct Proof

**Definition** - When trying to prove the proposition $p \rightarrow q$, we assume p is true, use rules of inference, logical equivalences, axioms, t show q must be true.

**Even:** An integer `n` is even means $n = 2*k$, where `k` is an integer.

**Odd:** An integer `n` is odd means there exists an integer `k` such that $n = 2k + 1$.

**Perfect Square:** An integer n is a perfect square means $\exists$ an integer $k$ such that $k^2 = n$.

#### Examples

**Example 1:** Prove the statement "If n is an odd integer, then n² is also odd."

-**PF**-
Assume that n is odd.
Therefore, $\exists$ an integer k such that $n = 2k + 1$.
*Want to show $n^2$ is odd, as in, want to find some integer $L$ such that $n^2 = 2k + 1$.*
Note, $n^2 = (n)(n) = (2k + 1)(2k + 1) = 4k^2 + 4k + 1$.
Since $4k^2 + 4k + 1 = 2(2k^2 + 2k) + 1$.
Letting $L = (2k^2 + 2k)$, we see $n^2 = 2L + 1$ and $L$ is an integer (because k is an integer) so $n^2$ is odd.

**Example 2:** Prove "If n & m are both perfect squares then $(nm)$ is a perfect square."

# ===FIX THIS LATER, DON'T WANT TO FIND TWO DIFFERENT INTEGERS, WANT TO FIND ONE INTEGER J THAT IS EQUIVALENT TO THE SQRT OF nm===

**-MY PF-** 
Assume that n & m are both perfect squares.
Therefore, $\exists$ an integer $k$ such that $n = k^2$ and $\exists$ an integer $L$ such that $m = L^2$.
Want to show that $(nm)$ is a perfect square, as in, want to find some integer $k$ and some integer $L$ such that $(nm) = (k^2L^2)$
Note, $n = (k)(k)$ and $m = (L)(L)$, thus $(nm) = (k^2L^2) = (k)(k)(L)(L)$.
Since both $k$ and $L$ are integers and since $k^2$ and $L^2$ are perfect squares, $(nm)$ is a perfect square.

**-MATT's PF-**
Assume n & m are perfect squares.
Then $\exists k, L \in \mathbb{Z}$ such that $n = k^2$ and $m = L^2$. 
We want to show that $nm$ is a perfect square as in we want to show $\exists a \in \mathbb{Z}$ such that $(nm) = a^2$.
Note $(k)(k) = n$ & $(L)(L) = m$, therefore $(nm) = n * m = (k)(k)(L)(L) = (k)(L)(k)(L)$.
Let $a = (k)(L)$.
Then, $(nm) = a^2$ and $a$ is an integer (b/c $k$ and $L$ are integers).

---
### 2. Proof by Contraposition

### $p \rightarrow q$

**Prove this directly instead: ** $\lnot q \rightarrow \lnot p$

#### Examples

**Example 3:** If n is an integer ($n \in \mathbb{Z}$), and 3n + 2 is odd, then n is odd.

- It is very inconvenient to prove this directly. You get $n = (2k - 1)/3$ which is hard to work with.

**-PF-**
Assume that $n$ is even.
We want to show $3n + 2$ is even.
We want to show $\exists k \in \mathbb{Z}$ such that $3n + 2 = 2k$.
Since $n$ is even, $\exists j \in \mathbb{Z}$ such that $n = 2j$.
Now, $3n + 2 = 3(2j) + 2 = 6j + 2 = 2(3j + 1)$.
Let $k = 3j + 1$, then we see $3n + 2$ is even.
Therefore, if $n \in \mathbb{Z}$, and $3n + 2$ is odd, then $n$ is odd.

**Example 4:** If n = ab, where a, b are positive integers, then a ≤ √n and b ≤ √n.

# ===DO THIS AS PRACTICE!===

**Rational and Irrational:** 
A real number $r$ is called rational if there exists integers $a, b$ such that $r = a/b$, and $b \neq 0$. 
A real # $q$ is irrational, if it is not rational. (weird ass definition)

**Example 5:** Prove that the sum of two rational numbers is rational.

**-PF-**
Let $r$ and $s$ be rational numbers.
Want to show that $r + s$ is rational.
Since $r$ and $s$ are rational, there exists integers $a,b,c,d$ with $b,d \neq 0$, such that $r = a/b$ and $s = c/d$.
Note, $r + s = \frac{a}{b} + \frac{c}{d} = \frac{ad + bc}{bd}$, and $ad + bc$ and $bd$ are integers and $bd \neq 0$ because neither b nor d were 0.
Hence r + s is rational.


**Example 6:** Prove that if n is an integer and n² is odd, then n is odd.

**-PF-**
Assume that $n \in \mathbb{Z}$ and $n^2$ is odd.
Want to show that n is odd, meaning $\exists k \in \mathbb{Z}$ such that n = 2k + 1.
We know $n^2 = 2j + 1$ since $n^2$ is odd.

===THE ABOVE ISNT WORKING. WE WOULD NEED TO GET A SQRT BUT THAT TAKES US NOWHERE. WE NEED TO DO A CONTRAPOSITIVE===

Contrapositive of the statement is if n is even, then prove that n is an integer and $n^2$ is even.
**-PF-**
By way of contarpositive, assume $n \in \mathbb{Z}$ is even. 
Want to show $n^2$ is even, meaning $\exists k \in \mathbb{Z}$ $n^2 = 2k$.
Since n is even, $\exists j \in \mathbb{Z}$ such that $n = 2j$.
Note $n^2 = n * n = (2j)(2j) = (2j)^2 = 4j^2$.
Letting $k = 2j^2$, we see $n^2 = 2k$.
So, $n^2$ is even.
Therefore if $n^2$ is odd then $n$ is odd.


---

### 3. Proof by Contradiction

#### For a Proposition p:

Using a direct proof, and showing that "math breaks", meaning a false statement must be True too.

**Example 7:** Prove that √2 is irrational.

**-PF-**
Towards a contradiction, assume $\sqrt{2}$ is rational.
Then $\exists a,b \in \mathbb{Z}$ with $b \neq 0$ such that $\sqrt{2} = a/b$, now cancel any common factors in $a$ & $b$.
So $\sqrt{2} = a'/b'$ where $a'/b'$ is in simplest form.
Then $(\sqrt{2})^2 = (a'/b')^2$.
Therefore 2 = $(a')^2/(b')^2$.
Therefore $2(b')^2 = (a')^2$.
Therefore $(a')^2$ is even because $2 * \mathbb{Z}$ is even.
Therefore $a'$ is even (since even # * even # = even # and odd # * odd # = odd #).
Therefore $a' = 2k$ for some $k \in \mathbb{Z} \implies 2(b')^2 = (2k)^2 \implies 2(b')^2 = 4k^2 \implies (b')^2 = 2k^2 \implies (b')^2$ is even $\implies b'$ is even.
But $a'/b'$ can't be in simplest form if $a'$ and $b'$ are both even, so $\sqrt{2}$ cannot be rational.
#### For an Implication p → q:

$p \rightarrow q \equiv \lnot p \lor q \rightarrow \lnot(\lnot p \lor q) \equiv p \lor \lnot q$

**Example 8:** The sum of an irrational and rational is irrational.

**-PF-**
Let $x \in \mathbb{R}$ be irrational, and let $y \in \mathbb{Q}$.
We want to show $x + y$ is irrational. 
Towards a contradiction, assume that $x + y$ is rational.
But if $x + y$ is rational, $\exists a,b \in \mathbb{Z}$ with $b \neq 0$ such that $x + y = \frac{a}{b}$.
Since y is rational, $\exists c,d \in \mathbb{Z}$, with $d \neq 0$  such that $y = \frac{c}{d}$.
Note, $x = (x + y) - y = \frac{a}{b} - \frac{c}{d} = \frac{ad-bc}{bd}$.
Hence, x must be rational, but this contradicts our assumption that x was irrational.
So, $x + y$ cannot be rational.

---

## Advanced Proof Techniques

### Proving Biconditionals

Remember: $p \leftrightarrow q \equiv (p \rightarrow q) \land (q \rightarrow p)$
>To prove $p \leftrightarrow q$, we need to prove both $p \rightarrow q$ and $q \rightarrow p$.

**Example 9:** If n is an integer, prove that n is odd iff n² is odd.
**AKA:** Let $n \in \mathbb{Z}$. Then $n$ is odd $\iff$ (iff, if and only if) $n^2$ is odd.
**This means:** (n is odd $\rightarrow n^2$ is odd) $\land$ ($n^2$ is odd $\rightarrow n$ is odd)
$n^2$ is odd $\rightarrow n$ is odd $\equiv$ n is even $\rightarrow n^2$ is even

### Proving Multiple Equivalences

**Hypothetical example:**
1. p
2. q
3. r

These are equivalent
$p \rightarrow q \rightarrow r$
and
$\begin{align} p &\rightarrow r \\ r &\rightarrow q \\ \hline \therefore p &\rightarrow q\end{align}$

The above is a hypothetical syllogism

**Example 10:** Show TFAE (The Following Are Equivalent):

- (a) n is even
- (b) n - 1 is odd
- (c) n² is even

**-PF-**
($1 \rightarrow 2$) Assume $n$ is even. Then $\exists k \in \mathbb{Z}$ such that $n = 2k$. Then note $n - 1 = 2(k-1) + 1$, hence $n - 1$ is odd.
($2 \rightarrow 3$) Assume n - 1 is odd. Then $n = 2k + 2$. Then $n^2 = (2k + 2)^2 = 4k^2 + 8k + 4 = 2(2k^2 + 4k + 2)$. So, $n^2$ is even.
($3 \rightarrow 1$) Assume $n^2$ is even. By the contrapositive of example 1, this is true.

(**Example 1:** Prove the statement "If n is an odd integer, then n² is also odd.")

---

## Tags

#mathematics #proofs #logic #discrete-math #math381

## Related Topics

- [[Logical Equivalences]]
- [[Rules of Inference]]
- [[Number Theory]]
- [[Mathematical Logic]]

---
# Proof Methods Explained Simply

## **Direct Proof**

**What it is:** Start with what you know, then use logical steps to reach what you want to prove.

**How it works:** If you want to prove "If P, then Q," you assume P is true and show that Q must follow.

**Simple example:** Prove "If a number is even, then its square is even."

- Start: Assume n is even, so n = 2k for some integer k
- Work forward: n² = (2k)² = 4k² = 2(2k²)
- Conclusion: Since n² = 2(2k²), it's even ✓

**Think of it like:** Following a recipe step-by-step from ingredients to finished dish.

---

## **Proof by Contraposition**

**What it is:** Instead of proving "If P, then Q," prove "If not Q, then not P" (which is logically equivalent).

**How it works:** Assume Q is false and show that P must also be false.

**Simple example:** Prove "If n² is even, then n is even."

- Instead, prove: "If n is odd, then n² is odd"
- Start: Assume n is odd, so n = 2k + 1
- Work: n² = (2k + 1)² = 4k² + 4k + 1 = 2(2k² + 2k) + 1
- Conclusion: n² is odd, so our contrapositive is proven ✓

**Think of it like:** Proving "All cats are mammals" by showing "If it's not a mammal, it's not a cat."

---

## **Proof by Contradiction**

**What it is:** Assume the opposite of what you want to prove, then show this leads to something impossible.

**How it works:** Assume your statement is false, derive a contradiction, conclude your statement must be true.

**Simple example:** Prove "There are infinitely many prime numbers."

- Start: Assume there are only finitely many primes
- Work through the logic: If so, we could list them all and create a new number that's also prime
- Contradiction: This new prime wasn't in our "complete" list!
- Conclusion: Our assumption was wrong, so there must be infinitely many primes ✓

**Think of it like:** Proving someone is innocent by showing that assuming they're guilty leads to impossible conclusions.

The key difference: **Direct** goes straight forward, **contraposition** goes backward from the opposite conclusion, and **contradiction** assumes you're wrong and finds the impossibility.